# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Proxy Configuration (for regions where Anthropic API is blocked)
USE_PROXY=false
# PROXY_URL=socks5://localhost:1080
# For Docker: PROXY_URL=socks5://host.docker.internal:1080

# Database Configuration (for analytics)
DB_HOST=localhost  # Using socat proxy
DB_PORT=5432
DB_NAME=analytics
DB_USER=analytics_user
DB_PASSWORD=analytics_secret_2024

# Real PostgreSQL host (for socat proxy)
POSTGRES_REAL_HOST=10.0.1.7  # Or aggw440gs08404s0kos4ocw8

# Environment
ENVIRONMENT=development
LOG_LEVEL=INFO